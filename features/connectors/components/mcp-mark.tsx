import { Cable, type LucideIcon } from "lucide-react";

/** The app's mark for connectors. A server draws its preset icon (mcp.const); this is the domain itself. */
export const McpMark: LucideIcon = Cable;
