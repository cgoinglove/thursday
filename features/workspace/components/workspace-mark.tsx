import { FolderOpen, type LucideIcon } from "lucide-react";

/** The app's mark for the workspace. Files inside draw their own kind (workspace-setting). */
export const WorkspaceMark: LucideIcon = FolderOpen;
