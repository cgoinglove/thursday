import { type LucideIcon, Package } from "lucide-react";

/**
 * The app's mark for what the bots finished. A box that was packed and handed
 * over — Workspace's open folder is the room it was made in, not the delivery.
 */
export const ArtifactMark: LucideIcon = Package;
