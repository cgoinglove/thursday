import { BookMarked, type LucideIcon } from "lucide-react";

/** The app's mark for skills. A shipped skill draws its own (skills-setting); this is the domain itself. */
export const SkillsMark: LucideIcon = BookMarked;
