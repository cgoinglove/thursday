# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

*.db

# agent workspace (local; holds the user's installed skills)
/.ai-workspace
# editor agent tooling, not the app's
/.agents
.playwright-cli
.playwright
.parcel-cache
.vercel
.design
skills-lock.json
*.local.*
# the publish tree (scripts/pack.mts)
/dist
